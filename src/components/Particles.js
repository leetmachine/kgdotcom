import React from 'react';
import styled from 'styled-components';

const Section = styled('section')`
height: 300px;
width: 100vw;
position: fixed;
top: 0;
`

const Particles = () => {
    return (
        <Section>
            {
                //particleJS goes here
            }
        </Section>
    );
};

export default Particles;